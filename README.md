# Edge AI Load Optimization — Monorepo (Skeleton)
Folders map to team roles:
- `infra/` — (Member A) cluster, monitoring, load tests
- `workloads/` — (Member B) demo services
- `ml/` — (Member B) forecasting service
- `controller/` — (Member C) decision engine
- `tests/` — (Member A)
- `dashboards/` — (Member A)
- `docs/` — (Member C)

Branches:
- `main` (protected)
- `develop`
- feature branches: `feat/<area>-<task>`
