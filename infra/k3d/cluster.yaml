apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: edge-lab
servers: 1
agents: 2
ports:
  - port: 80:80@loadbalancer
options:
  k3d:
    wait: true
  k3s:
    extraArgs:
      - arg: --disable=traefik
        nodeFilters:
          - server:0
